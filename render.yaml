services:
  - type: web
    name: incaseforh
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
